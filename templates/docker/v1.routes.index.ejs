'use strict';

const { <%= name %> } = require('../controllers');
//const schemas = require('../schemas');

const get = {
  path: '/v1/<%= name %>',
  method: 'GET',
  config: {
    description: 'Description text here',
    notes: 'Describe your notes here',
    tags: ['api'],
    handler: <%= name %>,
    validate: {
      options: {
        allowUnknown: true,
      },
      //query: schemas.<%= name %>.query,
    },
  },
};

module.exports = [
  get,
];
