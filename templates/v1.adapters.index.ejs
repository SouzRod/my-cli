'use strict';

const sampleWrapper = require('./sample');

module.exports = dependencies => ({
  sample: sampleWrapper({
    config: dependencies.config,<% if (resource.includes('mongo')){ %>
    repositoryMongoDb: dependencies.repositoryMongoDb,<% } %>
  }).handler,
});
