'use strict';

const factory = require('./factory');
const mongo = require('../../commons/mongo');
const config = require('../../config');

const testCollection = factory({
  db: mongo,
  collectionName: config.mongo.collections.testCollection,
  configConnection: {
    uri: config.mongo.uri,
    base: config.mongo.base,
  },
});

module.exports = {
  testCollection,
};
